package com.VolunTrack.demo.Notifications.Domain.Model.Queries;

public record GetAllNotificationsQuery() {
    /**
     * The GetAllNotificationsQuery class represents a query to retrieve all notifications from the system.
     * It does not require any parameters as it is used to fetch all notifications in the system.
     * This query is part of the **Query side** in CQRS (Command Query Responsibility Segregation) architecture.
     */
}
